# SPDX-License-Identifier: MPL-2.0
# This workflow is managed by gh actions-lock.
name: Hypatia Security Scan

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master]
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:

permissions:
  actions: read
  contents: read

jobs:
  scan:
    permissions:
      actions: read
      contents: read
      # Required by upload-sarif in the pinned reusable workflow.
      security-events: write
    uses: hyperpolymath/standards/.github/workflows/hypatia-scan-reusable.yml@e977cc67456de8349dcc08143b92ce5608083f67
    secrets: inherit
